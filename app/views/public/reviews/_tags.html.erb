<ul class="list-group border">
  <% tags.each do |tag| %>
  <li class="list-group-item">
    <%= link_to tag.name , search_tag_path(id: tag.id)%>
    <%="(#{tag.review.count})" %>
  </li>
  <% end %>
</ul>